package no.sigurof.tutorial.lwjgl.model

import no.sigurof.tutorial.lwjgl.textures.Texture

class TexturedModel constructor(rawModel: RawModel, texture: Texture) {
    val rawModel: RawModel = rawModel
    val texture: Texture = texture


}